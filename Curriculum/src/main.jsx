import React from 'react'
import ReactDOM from 'react-dom/client'
import {PrincipalComponent} from "./app/components/principalComponent";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
   <PrincipalComponent/>
  </React.StrictMode>,
)
